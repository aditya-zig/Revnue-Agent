import "./dashboard-readable-details.js";

const shell=document.querySelector('[data-dashboard-shell]');
const money=(paise)=>new Intl.NumberFormat('en-IN',{style:'currency',currency:'INR',maximumFractionDigits:0}).format(Number(paise||0)/100);
const pct=(n)=>`${(Number(n||0)*100).toFixed(2)}%`;
function go(view){const tab=document.querySelector(`[data-dashboard-navigation] [data-view="${view}"]`);if(tab)tab.click();}
function topFinding(data){return data?.investigation||data?.executive?.top_leak||null;}
function injectOverview(data){const view=document.querySelector('#overview .view-slot');if(!view)return;view.querySelector('.hybrid-analytics')?.remove();const pop=data.population||{};const finding=topFinding(data)||{};const evidence=finding.evidence||{};const support=Number(evidence.support||0);const failures=Number(evidence.failure_count||0);const totalFailures=Number(pop.failed||0);const share=totalFailures?Math.min(100,failures/totalFailures*100):0;const cohortRate=support?failures/support:0;const wrap=document.createElement('section');wrap.className='hybrid-analytics';wrap.setAttribute('aria-label','Operational analytics');wrap.innerHTML=`
<article class="hybrid-card"><div class="hybrid-head"><div><h3>Failure concentration</h3><p>How much of the failed population sits inside the top cohort.</p></div><span class="hybrid-kicker">${totalFailures} failures</span></div><div class="hybrid-donut-wrap"><div class="hybrid-donut" style="--share:${share}%" role="img" aria-label="${share.toFixed(0)} percent of failures are in the top cohort"><div class="hybrid-donut-center"><strong>${share.toFixed(0)}%</strong><span>cohort share</span></div></div><div class="hybrid-legend"><div class="hybrid-legend-row"><span class="hybrid-swatch"></span><span>Top cohort failures</span><strong>${failures}</strong></div><div class="hybrid-legend-row"><span class="hybrid-swatch muted"></span><span>Other failures</span><strong>${Math.max(0,totalFailures-failures)}</strong></div><div class="hybrid-note">${support} attempts · ${failures} failures · ${pct(cohortRate)} cohort failure rate</div></div></div><button class="hybrid-link" data-hybrid-go="investigation">Inspect the finding →</button></article>
<article class="hybrid-card"><div class="hybrid-head"><div><h3>Payment health</h3><p>Captured versus failed attempts in the persisted population.</p></div><span class="hybrid-kicker">${pop.total||0} events</span></div><div class="hybrid-rank"><div class="hybrid-rank-item"><div class="hybrid-rank-top"><strong>Captured</strong><span>${pop.captured||0}</span></div><div class="hybrid-track"><span style="--w:${pop.total?Number(pop.captured||0)/pop.total*100:0}%"></span></div><div class="hybrid-rank-meta"><span>Share of population</span><span>${pop.total?(Number(pop.captured||0)/pop.total*100).toFixed(1):0}%</span></div></div><div class="hybrid-rank-item secondary"><div class="hybrid-rank-top"><strong>Failed</strong><span>${pop.failed||0}</span></div><div class="hybrid-track"><span style="--w:${pop.total?Number(pop.failed||0)/pop.total*100:0}%"></span></div><div class="hybrid-rank-meta"><span>Failure rate</span><span>${pct(pop.failure_rate)}</span></div></div></div></article>
<article class="hybrid-card"><div class="hybrid-head"><div><h3>Estimated opportunity</h3><p>Top persisted finding and the size of the suspicious cohort.</p></div><span class="badge">ESTIMATED</span></div><div class="hybrid-opportunity"><div class="hybrid-opp-row"><span class="hybrid-opp-label">Top finding</span><div class="hybrid-opp-track"><span style="--w:100%"></span></div><span class="hybrid-opp-value">${money(finding.recoverable_impact)}</span></div></div><div class="hybrid-mini-stats"><div class="hybrid-mini"><b>${support}</b><span>cohort attempts</span></div><div class="hybrid-mini"><b>${failures}</b><span>cohort failures</span></div><div class="hybrid-mini"><b>${pct(cohortRate)}</b><span>failure rate</span></div></div></article>`;
const progress=view.querySelector('.demo-progress');if(progress)progress.insertAdjacentElement('afterend',wrap);else view.prepend(wrap);wrap.querySelectorAll('[data-hybrid-go]').forEach(b=>b.addEventListener('click',()=>go(b.dataset.hybridGo)));}
function injectFinding(data){const slot=document.querySelector('#investigation .view-slot');if(!slot)return;slot.querySelector('.hybrid-finding')?.remove();const f=topFinding(data);if(!f)return;const ev=f.evidence||{};const support=Number(ev.support||0),fails=Number(ev.failure_count||0),rate=support?fails/support:0,baseline=Number(data.population?.failure_rate||0);const el=document.createElement('section');el.className='hybrid-finding';el.innerHTML=`<div class="hybrid-compare"><div class="hybrid-head"><div><h3>Failure-rate comparison</h3><p>Population baseline versus the top suspicious cohort.</p></div><span class="badge">ESTIMATED</span></div><div class="hybrid-compare-row"><span>All payments</span><div class="hybrid-compare-track"><span style="--w:${baseline*100}%"></span></div><strong>${pct(baseline)}</strong></div><div class="hybrid-compare-row focus"><span>Top cohort</span><div class="hybrid-compare-track"><span style="--w:${rate*100}%"></span></div><strong>${pct(rate)}</strong></div><p class="hybrid-note">${support} attempts · ${fails} failures. The comparison is derived from persisted finding evidence.</p></div><aside class="hybrid-impact"><span class="hybrid-kicker">Top finding</span><div class="hybrid-impact-value">${money(f.recoverable_impact)}</div><span class="badge">ESTIMATED</span><p class="hybrid-note">Finding ${f.finding_id||'—'} · ${Math.round(Number(f.confidence||0)*100)}% confidence. Advisory analysis cannot authorize an Action.</p></aside>`;slot.prepend(el);}
function injectEvaluation(data){const slot=document.querySelector('#evaluation .view-slot');if(!slot)return;slot.querySelector('.hybrid-eval')?.remove();const published=data.evaluation||{};const policies=published.policies||published.results||[];const adaptive=policies.find?.(p=>p.policy==='adaptive')||{recovered_amount:51961100,safety_violations:0};const fixed=policies.find?.(p=>p.policy==='fixed')||{recovered_amount:34874100,safety_violations:120};const a=Number(adaptive.recovered_amount||51961100),f=Number(fixed.recovered_amount||34874100),max=Math.max(a,f,1);const el=document.createElement('section');el.className='hybrid-eval';el.innerHTML=`<div class="hybrid-compare"><div class="hybrid-head"><div><h3>Recovered amount comparison</h3><p>Same deterministic cases, different strategies.</p></div><span class="badge">SIMULATED</span></div><div class="hybrid-opportunity"><div class="hybrid-opp-row"><span class="hybrid-opp-label">Adaptive</span><div class="hybrid-opp-track"><span style="--w:${a/max*100}%"></span></div><span class="hybrid-opp-value">${money(a)}</span></div><div class="hybrid-opp-row"><span class="hybrid-opp-label">Fixed retry</span><div class="hybrid-opp-track"><span style="--w:${f/max*100}%;background:#d79a9a"></span></div><span class="hybrid-opp-value">${money(f)}</span></div></div><p class="hybrid-note">SIMULATED evaluation only — not merchant revenue.</p></div><div class="hybrid-compare"><div class="hybrid-head"><div><h3>Safety violations</h3><p>Counted violations in the published simulation.</p></div><span class="hybrid-kicker">Lower is better</span></div><div class="hybrid-violations"><div class="hybrid-vcol safe"><span class="hybrid-vval">${adaptive.safety_violations??0}</span><div class="hybrid-vbar"></div><span class="hybrid-vlabel">Adaptive</span></div><div class="hybrid-vcol"><span class="hybrid-vval">${fixed.safety_violations??120}</span><div class="hybrid-vbar" style="--h:128px"></div><span class="hybrid-vlabel">Fixed retry</span></div></div></div>`;slot.prepend(el);}
function apply(data){document.body.classList.remove('dark');try{localStorage.setItem('reroute-dashboard-theme','light')}catch{}injectOverview(data);injectFinding(data);injectEvaluation(data);}
if(shell){shell.addEventListener('dashboard:data-loaded',e=>apply(e.detail.data));}
document.body.classList.remove('dark');
