# RecoveryCase lifecycle

ReRoute automatically marks a fully evidenced case eligible but requires a human to start the first Action. Each Action moves the case to awaiting outcome, applies a 24-hour limit, and cannot exceed three customer contacts for the permanent case. Provider failures and help requests escalate to a human, while verified payment recovers and stop conditions close the case permanently.
