# Customer communication

ReRoute models mock email only and never sends real mail. Every recovery email needs explicit RecoveryContactConsent, Policy permission, and human approval. It records audit-linked inbound and outbound messages, escalates delivery failures and ambiguous replies, and lets Customers opt out without further recovery contact.
